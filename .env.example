# Environment Configuration for KMTIFMS2
# Copy this file to .env and update with your settings

# =============================================================================
# NODE ENVIRONMENT
# =============================================================================
NODE_ENV=production
# Options: development, production

# =============================================================================
# MySQL DATABASE CONFIGURATION
# =============================================================================

# Database Host (MySQL server hostname or IP)
DB_HOST=KMTI-NAS
# For local development: localhost
# For production: KMTI-NAS or server IP address

# Database Port
DB_PORT=3306
# Default MySQL port is 3306

# Database Name
DB_NAME=kmtifms
# For development: kmtifms_dev

# Database User
DB_USER=kmtifms_user
# IMPORTANT: Create this user in MySQL with proper permissions

# Database Password
DB_PASSWORD=your-secure-password-here
# IMPORTANT: Change this to a strong, secure password!
# Recommendation: Use minimum 16 characters with mixed case, numbers, and symbols

# =============================================================================
# SERVER CONFIGURATION
# =============================================================================

# Server Port (Express server)
SERVER_PORT=3001

# =============================================================================
# SECURITY NOTES
# =============================================================================
# 1. Never commit the .env file to version control
# 2. Use different credentials for development and production
# 3. Use strong passwords for all database users
# 4. Restrict database user access to specific IP ranges in production
# 5. Enable SSL/TLS for database connections in production

# =============================================================================
# SETUP INSTRUCTIONS
# =============================================================================
# 1. Copy this file: cp .env.example .env
# 2. Update DB_HOST with your MySQL server hostname
# 3. Update DB_PASSWORD with a secure password
# 4. Create MySQL database and user:
#    CREATE DATABASE kmtifms;
#    CREATE USER 'kmtifms_user'@'%' IDENTIFIED BY 'your-password';
#    GRANT ALL PRIVILEGES ON kmtifms.* TO 'kmtifms_user'@'%';
#    FLUSH PRIVILEGES;
# 5. Initialize database: npm run db:init
# 6. Test connection: npm run db:test
