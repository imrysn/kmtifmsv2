/**
 * DEPRECATED — now handled automatically by migrations 012 & 013 on every server start.
 * Safe to delete this file.
 */
console.log('This fix is now handled by migrations 012 & 013. No action needed.');
process.exit(0);
