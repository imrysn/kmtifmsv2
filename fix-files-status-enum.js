/**
 * DEPRECATED — now handled automatically by migration 012 on every server start.
 * Safe to delete this file.
 */
console.log('This fix is now handled by migration 012 (012-fix-files-status-column.js). No action needed.');
process.exit(0);
